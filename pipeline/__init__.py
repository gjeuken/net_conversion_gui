"""Compute core for the catabolic-pathway net-conversion / EFM / ΔG pipeline.

Each module holds pure functions ported from the accompanying notebook
(``educational_paper_pipeline.ipynb``).  Nothing here imports Dash; the UI
layer (``app.py``) and the CLI (``cli.py``) call into these modules.
"""
